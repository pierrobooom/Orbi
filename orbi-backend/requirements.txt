fastapi==0.115.0
uvicorn[standard]==0.30.0
supabase==2.5.0
psycopg2-binary==2.9.11
pgvector==0.3.2
pydantic[email]==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.0.1
httpx==0.27.0
groq==0.9.0
anthropic==0.34.0
deepgram-sdk==3.7.7
elevenlabs==1.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
# IANA timezone database for Python's zoneinfo. Windows doesn't ship
# one natively — without this, ZoneInfo("Europe/London") raises and
# every wall-clock-to-UTC conversion silently fails. Required even
# though it's a noop on Linux/macOS which already have system tzdata.
tzdata==2026.2
pytest==8.2.0
pytest-asyncio==0.23.7
